'use client';

import React from 'react';

// Simplified diagnostics initializer to avoid compilation issues  
const DiagnosticsInitializer: React.FC = () => {
  return null; // This component doesn't render anything
};

export default DiagnosticsInitializer;