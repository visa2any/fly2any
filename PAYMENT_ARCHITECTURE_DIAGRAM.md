# Payment Integration - Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           FLY2ANY PAYMENT SYSTEM                             │
│                         PCI SAQ-A Compliant Architecture                     │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Complete Data Flow

```
┌──────────────────┐
│                  │
│  1. USER BROWSER │  (Client-Side)
│                  │
└────────┬─────────┘
         │
         │ 1. Search & Select Flight
         │
         ▼
┌────────────────────────────────────────────────────────────────────┐
│                     BOOKING FLOW (3 Steps)                         │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  STEP 1: Customize Flight                                         │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ - Select Fare (Basic/Standard/Flex/Business)            │   │
│  │ - Choose Bundle (ML-powered packages)                   │   │
│  │ - Add Individual Add-ons (Seats, Baggage, Insurance)   │   │
│  │ - View Seat Map                                         │   │
│  │ - Calculate Total Price                                 │   │
│  └──────────────────────────────────────────────────────────┘   │
│         │                                                         │
│         │ Continue                                                │
│         ▼                                                         │
│  STEP 2: Enter Passenger Details                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ - Passenger Names, DOB, Nationality                     │   │
│  │ - Passport Info (International flights)                 │   │
│  │ - Contact Email & Phone                                 │   │
│  │ - Validate All Fields                                   │   │
│  └──────────────────────────────────────────────────────────┘   │
│         │                                                         │
│         │ Continue                                                │
│         ▼                                                         │
│  STEP 3: Review & Payment                                        │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ - Review Flight Summary                                  │   │
│  │ - Review Passenger List                                  │   │
│  │ - DOT Compliance Checkboxes                             │   │
│  │ - Stripe Payment Form                                    │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                    │
└────────┬───────────────────────────────────────────────────────────┘
         │
         │ 2. Create Booking (Status: Pending)
         │
         ▼
┌────────────────────────────────────────────────────────────────────┐
│  YOUR SERVER (Next.js API Routes)                                 │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  POST /api/bookings                                               │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 1. Validate booking data                                 │   │
│  │ 2. Generate booking reference (FLY2A-XXXXXX)            │   │
│  │ 3. Store in PostgreSQL (Status: Pending)               │   │
│  │ 4. Return: { bookingId, bookingReference }             │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                    │
└────────┬───────────────────────────────────────────────────────────┘
         │
         │ 3. Request Payment Intent
         │    { amount, currency, bookingReference, email }
         │
         ▼
┌────────────────────────────────────────────────────────────────────┐
│  YOUR SERVER                                                       │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  POST /api/payments/create-intent                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 1. Validate amount, currency, booking ref               │   │
│  │ 2. Rate limit check (5/min per IP)                     │   │
│  │ 3. Call Stripe API ───────────────────────┐            │   │
│  │ 4. Return client secret to browser        │            │   │
│  └────────────────────────────────────────────┼────────────┘   │
│                                               │                    │
└───────────────────────────────────────────────┼────────────────────┘
                                                │
                                                │ 4. Create Payment Intent
                                                │
                                                ▼
┌────────────────────────────────────────────────────────────────────┐
│  STRIPE SERVERS (PCI Level 1)                                     │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  stripe.paymentIntents.create()                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ - Create Payment Intent                                  │   │
│  │ - Amount: $599.99 → 59999 cents                         │   │
│  │ - Currency: USD                                          │   │
│  │ - Enable 3D Secure                                       │   │
│  │ - Attach metadata (booking ref)                         │   │
│  │ - Generate client secret                                │   │
│  │ - Return: { id: 'pi_xxx', client_secret: 'pi_xxx_secret_yyy' } │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                    │
└────────┬───────────────────────────────────────────────────────────┘
         │
         │ 5. Return Client Secret
         │
         ▼
┌──────────────────┐
│  USER BROWSER    │
├──────────────────┤
│                  │
│  Stripe Elements │  (Secure iframe - NO card data touches your server)
│  ┌──────────────┐
│  │ [Card Input] │  ← Hosted by Stripe (PCI-compliant)
│  │ 4242 4242... │
│  │ 12/25   123  │
│  │ [Pay $599]   │
│  └──────────────┘
│         │
│         │ 6. Submit Payment
│         │
│         ▼
│  stripe.confirmPayment()
│  ┌──────────────────────────────────────────────────────────┐
│  │ - Collect card data in iframe                            │
│  │ - Send directly to Stripe (NOT your server)             │
│  │ - Handle 3D Secure if required                          │
│  │ - Return payment intent status                          │
│  └──────────────────────────────────────────────────────────┘
│         │
└─────────┼────────────────────────────────────────────────────────────┘
          │
          │ 7. Process Payment
          │
          ▼
┌────────────────────────────────────────────────────────────────────┐
│  STRIPE SERVERS                                                    │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  Payment Processing                                               │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 1. Verify card with issuing bank                        │   │
│  │ 2. Check for fraud (Stripe Radar)                       │   │
│  │ 3. Trigger 3D Secure if required ──────────┐           │   │
│  │ 4. Authorize payment                        │           │   │
│  │ 5. Capture funds                            │           │   │
│  │ 6. Update payment intent status             │           │   │
│  └─────────────────────────────────────────────┼───────────┘   │
│                                                 │                   │
│                                    3D Secure    │                   │
│                                    ┌────────────▼────────────┐     │
│                                    │ Bank Auth Page          │     │
│                                    │ - SMS Code              │     │
│                                    │ - Biometric             │     │
│                                    │ - App Confirmation      │     │
│                                    └────────────┬────────────┘     │
│                                                 │                   │
└─────────────────────────────────────────────────┼───────────────────┘
          │                                       │
          │ 8a. Webhook (Async)                  │ 8b. Return to Browser
          │                                       │
          ▼                                       ▼
┌────────────────────────────────────┐   ┌──────────────────┐
│  YOUR SERVER                       │   │  USER BROWSER    │
│  POST /api/payments/webhook        │   │  Payment Success │
│  ┌────────────────────────────┐   │   │  Status: succeeded│
│  │ 1. Verify signature        │   │   │  Redirect to...  │
│  │ 2. Handle event:           │   │   └────────┬─────────┘
│  │    - payment_intent.       │   │            │
│  │      succeeded             │   │            │ 9. Confirm Payment
│  │ 3. Update booking status   │   │            │
│  │ 4. Send confirmation email │   │            ▼
│  └────────────────────────────┘   │   ┌────────────────────────┐
│                                     │   │  POST /api/payments/   │
└────────┬────────────────────────────┘   │       confirm          │
         │                                 │  ┌──────────────────┐ │
         │                                 │  │ 1. Verify payment│ │
         │                                 │  │ 2. Update booking│ │
         │                                 │  │ 3. Return success│ │
         │                                 │  └──────────────────┘ │
         │                                 └────────┬───────────────┘
         │                                          │
         │ 10. Update Database                     │
         │                                          │
         ▼                                          ▼
┌────────────────────────────────────────────────────────────────────┐
│  POSTGRESQL DATABASE (Neon)                                       │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  UPDATE bookings                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ SET status = 'confirmed'                                 │   │
│  │     payment = {                                           │   │
│  │       status: 'paid',                                     │   │
│  │       transactionId: 'pi_xxx',                           │   │
│  │       paidAt: '2025-06-01T10:05:00Z',                   │   │
│  │       cardLast4: '4242',                                 │   │
│  │       cardBrand: 'visa'                                  │   │
│  │     }                                                     │   │
│  │ WHERE booking_reference = 'FLY2A-ABC123'                │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                    │
└────────┬───────────────────────────────────────────────────────────┘
         │
         │ 11. Redirect to Confirmation
         │
         ▼
┌──────────────────┐
│  USER BROWSER    │
│  Confirmation    │
│  ┌──────────────┐
│  │ ✅ Booking   │
│  │    Confirmed!│
│  │              │
│  │ FLY2A-ABC123│
│  │              │
│  │ [Download]   │
│  └──────────────┘
└──────────────────┘
```

---

## Component Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                        FRONTEND COMPONENTS                          │
└─────────────────────────────────────────────────────────────────────┘

/flights/booking-optimized/page.tsx  (Main booking container)
    ├── Step 1: Customize
    │   ├── FareSelector.tsx
    │   ├── SmartBundles.tsx
    │   ├── AddOnsTabs.tsx
    │   └── SeatMapModal.tsx
    │
    ├── Step 2: Passengers
    │   └── CompactPassengerForm.tsx
    │       ├── Passenger #1 (Adult, Lead)
    │       ├── Passenger #2 (Adult)
    │       └── Contact Info (Email, Phone)
    │
    └── Step 3: Payment
        └── ReviewAndPay.tsx
            ├── Flight Summary
            ├── DOT Compliance Checkboxes
            └── StripePaymentForm.tsx  ← Stripe Elements
                ├── PaymentElement (Card input iframe)
                ├── Billing Address
                └── Submit Button


┌─────────────────────────────────────────────────────────────────────┐
│                         BACKEND SERVICES                            │
└─────────────────────────────────────────────────────────────────────┘

lib/payments/payment-service.ts  (Payment orchestration)
    ├── createPaymentIntent()
    ├── confirmPayment()
    ├── processRefund()
    ├── verifyWebhookSignature()
    └── handleWebhookEvent()

lib/bookings/storage.ts  (Database operations)
    ├── create()
    ├── findById()
    ├── findByReferenceAsync()
    ├── update()
    └── cancel()

lib/db/connection.ts  (PostgreSQL client - Neon)


┌─────────────────────────────────────────────────────────────────────┐
│                           API ROUTES                                │
└─────────────────────────────────────────────────────────────────────┘

app/api/bookings/
    ├── route.ts               (GET, POST - List/create bookings)
    └── [id]/route.ts          (GET, PUT, DELETE - Manage booking)

app/api/payments/
    ├── create-intent/route.ts (POST - Create payment intent)
    ├── confirm/route.ts       (POST - Confirm payment)
    └── webhook/route.ts       (POST - Stripe webhook handler)
```

---

## Security Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                        SECURITY LAYERS                              │
└─────────────────────────────────────────────────────────────────────┘

Layer 1: CLIENT-SIDE SECURITY
┌──────────────────────────────────────────────────────────────┐
│ ✅ HTTPS Only (Vercel enforces)                             │
│ ✅ Stripe Elements (Secure iframe - no card data in DOM)   │
│ ✅ CSP Headers (Prevent XSS)                               │
│ ✅ Input validation (UX only)                              │
│ ✅ No sensitive data in localStorage/sessionStorage        │
└──────────────────────────────────────────────────────────────┘
            │
            │ All communication encrypted (TLS 1.3)
            ▼
Layer 2: API GATEWAY SECURITY
┌──────────────────────────────────────────────────────────────┐
│ ✅ Rate limiting (5 req/min per IP)                         │
│ ✅ Input validation (server-side)                          │
│ ✅ Authentication (for user bookings)                      │
│ ✅ CORS configuration                                       │
│ ✅ Request size limits                                      │
└──────────────────────────────────────────────────────────────┘
            │
            │ Validated requests only
            ▼
Layer 3: APPLICATION SECURITY
┌──────────────────────────────────────────────────────────────┐
│ ✅ Environment variables (secrets)                          │
│ ✅ SQL injection prevention (parameterized queries)        │
│ ✅ XSS prevention (React escaping)                         │
│ ✅ CSRF protection (Next.js default)                       │
│ ✅ Error handling (no sensitive data in messages)          │
└──────────────────────────────────────────────────────────────┘
            │
            │ Secure data handling
            ▼
Layer 4: PAYMENT PROCESSING SECURITY
┌──────────────────────────────────────────────────────────────┐
│ ✅ Stripe API (PCI Level 1 certified)                      │
│ ✅ 3D Secure 2.0 (SCA compliance)                          │
│ ✅ Fraud detection (Stripe Radar)                          │
│ ✅ Webhook signature verification                          │
│ ✅ Payment intent idempotency                              │
└──────────────────────────────────────────────────────────────┘
            │
            │ Verified payments only
            ▼
Layer 5: DATABASE SECURITY
┌──────────────────────────────────────────────────────────────┐
│ ✅ Encrypted at rest (Neon default)                        │
│ ✅ Encrypted in transit (SSL)                              │
│ ✅ Minimal permissions (read/write bookings only)          │
│ ✅ No card data stored                                      │
│ ✅ Audit logging                                            │
└──────────────────────────────────────────────────────────────┘
```

---

## PCI Compliance Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                    PCI SAQ A COMPLIANCE MODEL                       │
│               (No Card Data Touches Your Servers)                   │
└─────────────────────────────────────────────────────────────────────┘

YOUR RESPONSIBILITY                    STRIPE'S RESPONSIBILITY
┌─────────────────────┐               ┌─────────────────────┐
│                     │               │                     │
│  • Use HTTPS        │               │  • Store card data  │
│  • Secure API keys  │               │  • Process payments │
│  • Validate inputs  │               │  • PCI Level 1      │
│  • Handle webhooks  │               │  • 3D Secure        │
│  • Log activities   │               │  • Fraud detection  │
│                     │               │  • Tokenization     │
└──────────┬──────────┘               └──────────▲──────────┘
           │                                     │
           │                                     │
           │  Card data flows directly to Stripe │
           │  (Never touches your server)        │
           │                                     │
           └─────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                        DATA STORAGE POLICY                          │
└─────────────────────────────────────────────────────────────────────┘

✅ SAFE TO STORE:
- Booking reference (FLY2A-ABC123)
- Customer email
- Customer name
- Card last 4 digits (4242)
- Card brand (Visa, Mastercard)
- Stripe Payment Intent ID (pi_xxx)
- Payment amount & currency
- Payment status (paid, pending, failed)
- Timestamp of payment

❌ NEVER STORE:
- Full card number
- CVV/CVC code
- Card expiry date
- Card PIN
- Any unencrypted cardholder data
```

---

## Error Handling Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                       ERROR HANDLING PATHS                          │
└─────────────────────────────────────────────────────────────────────┘

SUCCESS PATH (95% of payments)
User → Payment Form → Stripe → Bank → Success → Confirmation Page

FAILURE PATHS:

1. Card Declined
   User → Payment Form → Stripe → Bank → ❌ Declined
        → Show Error: "Card declined. Try different card?"
        → Allow retry (max 5 attempts)
        → Log decline reason

2. Insufficient Funds
   User → Payment Form → Stripe → Bank → ❌ Insufficient
        → Show Error: "Insufficient funds"
        → Suggest lower fare class or fewer add-ons
        → Allow retry

3. 3D Secure Required
   User → Payment Form → Stripe → 🔐 3DS Required
        → Redirect to bank auth page
        → User completes authentication
        → Return to confirmation
        → Continue payment

4. 3D Secure Failed
   User → Payment Form → Stripe → 🔐 3DS → ❌ Failed
        → Show Error: "Authentication failed"
        → Suggest contacting bank
        → Allow retry with different card

5. Network Error
   User → Payment Form → ❌ Network timeout
        → Show Error: "Connection lost. Please try again."
        → Retry with exponential backoff
        → Resume from last successful step

6. Webhook Delivery Failure
   Payment Success → Webhook → ❌ Delivery failed
        → Stripe retries (up to 3 days)
        → Manual reconciliation if all retries fail
        → Admin notification

7. Database Error
   Payment Success → Update DB → ❌ Database error
        → Payment still captured
        → Log error
        → Manual reconciliation
        → Send customer confirmation anyway
```

---

## Webhook Event Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                    STRIPE WEBHOOK EVENTS                            │
└─────────────────────────────────────────────────────────────────────┘

STRIPE SERVER                YOUR SERVER
     │                           │
     │  payment_intent.created   │
     ├──────────────────────────>│ Log: "Payment initiated"
     │                           │
     │                           │ [User completes payment]
     │                           │
     │  payment_intent.succeeded │
     ├──────────────────────────>│ 1. Verify signature
     │                           │ 2. Update booking: status = 'confirmed'
     │                           │ 3. Update payment: status = 'paid'
     │                           │ 4. Send confirmation email
     │                           │ 5. Return 200 OK
     │                           │
     │  charge.succeeded         │
     ├──────────────────────────>│ Log: "Charge captured"
     │                           │
     │                           │ [Later: User cancels]
     │                           │
     │  charge.refunded          │
     ├──────────────────────────>│ 1. Update booking: status = 'cancelled'
     │                           │ 2. Update payment: status = 'refunded'
     │                           │ 3. Send cancellation email
     │                           │ 4. Return 200 OK
     │                           │

ALTERNATIVE FLOWS:

payment_intent.payment_failed
     ├──────────────────────────>│ 1. Log failure reason
     │                           │ 2. Mark booking as failed
     │                           │ 3. Alert customer via email
     │                           │ 4. Allow retry

payment_intent.requires_action (3D Secure)
     ├──────────────────────────>│ 1. Log: "Awaiting 3DS"
     │                           │ 2. No action needed
     │                           │ 3. Wait for succeeded event
```

---

## Monitoring Dashboard

```
┌─────────────────────────────────────────────────────────────────────┐
│                      METRICS TO MONITOR                             │
└─────────────────────────────────────────────────────────────────────┘

PAYMENT METRICS
┌─────────────────────────────────────────────────────┐
│ Payment Success Rate:  97.5% ✅ (Target: >95%)     │
│ 3D Secure Rate:        15.2%    (Expected: 10-20%) │
│ Decline Rate:          2.3% ✅  (Target: <5%)      │
│ Average Payment Time:  2.8s ✅  (Target: <3s)      │
│ Webhook Delivery:      99.9% ✅ (Target: >99%)     │
└─────────────────────────────────────────────────────┘

REVENUE METRICS
┌─────────────────────────────────────────────────────┐
│ Today's Revenue:       $15,450                      │
│ Week's Revenue:        $78,320                      │
│ Month's Revenue:       $285,600                     │
│ Average Booking Value: $625                         │
│ Bookings Today:        24                           │
└─────────────────────────────────────────────────────┘

BOOKING FUNNEL
┌─────────────────────────────────────────────────────┐
│ Searches:              1,250   100%                 │
│ ├─> Results Viewed:      875    70%                │
│ ├─> Booking Started:     125    10% (of searches)  │
│ ├─> Payment Reached:      75     6%                │
│ └─> Payment Completed:    50     4% ✅             │
│                                                      │
│ Conversion Rate: 4.0% (Target: 3-5%)               │
│ Cart Abandonment: 40% (at payment)                 │
└─────────────────────────────────────────────────────┘

ERROR TRACKING
┌─────────────────────────────────────────────────────┐
│ 5xx Errors:             2  ✅ (Target: <10/day)    │
│ 4xx Errors:            15     (Mostly invalid input)│
│ Payment Failures:       3  ✅                       │
│ Database Errors:        0  ✅                       │
│ Webhook Failures:       1  ✅                       │
└─────────────────────────────────────────────────────┘
```

---

## Deployment Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                     PRODUCTION INFRASTRUCTURE                       │
└─────────────────────────────────────────────────────────────────────┘

VERCEL (Frontend + API)
┌───────────────────────────────────────────────┐
│  Edge Network (CDN)                           │
│  ├─> Static Assets (CSS, JS, Images)         │
│  └─> Next.js Server (API Routes)             │
│      ├─> /api/payments/create-intent         │
│      ├─> /api/payments/confirm               │
│      ├─> /api/payments/webhook               │
│      └─> /api/bookings/*                     │
└───────────────────────────────────────────────┘
            │
            │ Environment Variables:
            │ - STRIPE_SECRET_KEY
            │ - NEXT_PUBLIC_STRIPE_PUBLIC_KEY
            │ - STRIPE_WEBHOOK_SECRET
            │ - POSTGRES_URL
            │
            ▼
NEON (PostgreSQL Database)
┌───────────────────────────────────────────────┐
│  Serverless Postgres                          │
│  ├─> bookings table                           │
│  ├─> flight_bookings table                    │
│  └─> webhook_events table                     │
│                                                │
│  Features:                                     │
│  - Auto-scaling                                │
│  - Branching (dev/staging/prod)               │
│  - Point-in-time recovery                     │
│  - Encryption at rest                          │
└───────────────────────────────────────────────┘
            │
            │ Connection: SSL/TLS
            │
            ▼
STRIPE (Payment Processing)
┌───────────────────────────────────────────────┐
│  Payment Gateway                               │
│  ├─> Create Payment Intent                    │
│  ├─> Process Payment                          │
│  ├─> 3D Secure Authentication                 │
│  ├─> Fraud Detection (Radar)                  │
│  └─> Webhook Delivery                         │
│                                                │
│  Compliance:                                   │
│  - PCI Level 1                                 │
│  - SCA (Strong Customer Authentication)       │
│  - GDPR compliant                              │
└───────────────────────────────────────────────┘

MONITORING
┌───────────────────────────────────────────────┐
│  Sentry (Error Tracking)                      │
│  Better Uptime (API Monitoring)               │
│  Stripe Dashboard (Payment Analytics)         │
└───────────────────────────────────────────────┘
```

---

## Summary

### Data Flow Summary
1. **User searches** → Results displayed
2. **User books** → Booking created (pending)
3. **Payment intent created** → Stripe generates client secret
4. **User pays** → Card data sent directly to Stripe (NOT your server)
5. **Payment processed** → Stripe validates with bank (3DS if needed)
6. **Webhook sent** → Your server updates booking to "confirmed"
7. **Email sent** → Customer receives confirmation
8. **Complete** → User sees confirmation page

### Security Summary
- ✅ **PCI SAQ A** compliant (simplest level)
- ✅ **No card data** stored on your servers
- ✅ **Stripe Elements** for secure card input
- ✅ **3D Secure** for fraud prevention
- ✅ **Webhook verification** for data integrity
- ✅ **HTTPS only** for all communications

### Architecture Highlights
- **3-step checkout** (reduced from 7 steps)
- **Modular components** (easy to maintain)
- **Async processing** (webhooks for reliability)
- **Serverless scaling** (Vercel + Neon)
- **Production-ready** (comprehensive error handling)

**Ready for production deployment! 🚀**
