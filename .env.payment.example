# ================================================
# PAYMENT GATEWAY CONFIGURATION
# ================================================
# This file contains the environment variables needed for payment processing
# Copy this to your .env.local file and fill in your actual keys
# ================================================

# --------------------------------------------
# STRIPE PAYMENT GATEWAY
# --------------------------------------------
# Get these from: https://dashboard.stripe.com/apikeys

# Stripe Publishable Key (Client-side, safe to expose)
# Used in the frontend to initialize Stripe Elements
NEXT_PUBLIC_STRIPE_PUBLIC_KEY=pk_test_...

# Stripe Secret Key (Server-side, NEVER expose to client)
# Used for creating payment intents and processing refunds
STRIPE_SECRET_KEY=sk_test_...

# Stripe Webhook Secret (Server-side, NEVER expose to client)
# Get this from: https://dashboard.stripe.com/webhooks
# This is used to verify webhook signatures from Stripe
STRIPE_WEBHOOK_SECRET=whsec_...

# --------------------------------------------
# DUFFEL API (Already configured)
# --------------------------------------------
# Duffel Payments integration (when enabled)
# Get from: https://duffel.com/dashboard
DUFFEL_ACCESS_TOKEN=duffel_test_...

# ================================================
# WEBHOOK CONFIGURATION
# ================================================

# Your webhook endpoint URL (for Stripe webhook registration)
# In development: Use ngrok or similar tool to expose localhost
# In production: Use your production domain
# Example: https://your-domain.com/api/payments/webhook
WEBHOOK_URL=https://your-domain.com/api/payments/webhook

# ================================================
# TESTING
# ================================================

# Stripe Test Cards:
# - Success: 4242 4242 4242 4242
# - Requires 3D Secure: 4000 0025 0000 3155
# - Declined: 4000 0000 0000 0002
# - Insufficient Funds: 4000 0000 0000 9995

# Use any future expiration date (e.g., 12/25)
# Use any 3-digit CVV (e.g., 123)
# Use any ZIP code (e.g., 12345)

# ================================================
# SETUP INSTRUCTIONS
# ================================================

# 1. Create a Stripe account at https://stripe.com
# 2. Get your test API keys from https://dashboard.stripe.com/test/apikeys
# 3. Copy this file to .env.local and fill in your keys
# 4. For webhooks in development:
#    - Install ngrok: https://ngrok.com/download
#    - Run: ngrok http 3000
#    - Copy the HTTPS URL and register it in Stripe dashboard
#    - Add /api/payments/webhook to the URL
#    - Select events: payment_intent.succeeded, payment_intent.payment_failed
# 5. For production:
#    - Switch to live keys (pk_live_... and sk_live_...)
#    - Register your production webhook URL
#    - Ensure HTTPS is enabled on your domain

# ================================================
# SECURITY NOTES
# ================================================

# IMPORTANT:
# - NEVER commit .env.local to version control
# - NEVER expose secret keys in client-side code
# - Always use HTTPS in production
# - Regularly rotate your API keys
# - Monitor Stripe dashboard for suspicious activity
# - Enable Stripe Radar for fraud protection

# ================================================
